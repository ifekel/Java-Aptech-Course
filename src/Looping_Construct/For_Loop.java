package Looping_Construct;

public class For_Loop {
    public static void main(String[] args) {
        // The For Loop

        // int num, product;
        // The for loop with all three declaration parts

        // for (num = 1; num <= 5; num++) {
            // product = num * 10;
           // System.out.printf("\n%d * 10 = %d", num, product);
        // }
        // System.out.printf("\nThe current value in num is : %d", num);

        // Comma Operator
        // int i, j;
        // int max = 11;
        /**
         * The initialization and increment/decrement section includes
         * more than one variable.
         */
        // for (i = 0, j = max; i <= max; i++, j--){
            // System.out.printf("\n%d + %d = %d", i, j, i + j);
        // }

        // Example 3
        int num1 = 1;

        // A boolean variables initialized as false.
        boolean flag = false;

        // Create a for loop with only two initializations.
        /*
        for (; !flag; num1++) {
            System.out.println("Value of num1: " + num1);
            if (num1 == 100) {
                flag = true;
            }
        }
         */

    }
}